fastapi==0.135.3
uvicorn[standard]==0.44.0
python-multipart==0.0.26
pydub==0.25.1
librosa==0.11.0
numpy==2.4.4
scipy==1.17.1
# torch CPU-only — installed separately in Dockerfile via --index-url
# to avoid pulling in the full CUDA wheel (~900 MB → ~200 MB with CPU only)
torch==2.11.0
